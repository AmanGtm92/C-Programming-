#include<stdio.h>
int main()
{
    int ln,wd;
    printf("Enter the lenghth :");
    scanf("%d",&ln);

    printf("Enter the widhth :");
    scanf("%d",&wd);

    printf("the area is %d",ln*wd);

    return 0;
}